# 弹幕功能说明

## 功能概述

云上香项目新增了弹幕功能，用户可以在页面上发送弹幕，其他访问的用户可以看到弹幕效果。

## 功能特性

### 1. 发送弹幕
- 在页面底部输入框输入文字
- 点击"发送"按钮或按回车键发送
- 弹幕最长 50 个字符

### 2. 弹幕设置
点击弹幕输入框旁的设置按钮（⚙️）可打开弹幕设置面板，支持以下配置：

- **启用弹幕**：开关弹幕功能
- **弹幕透明度**：调整弹幕透明度（10%-100%）
- **字体大小**：调整弹幕字体大小（0.5x-2x）
- **弹幕速度**：调整弹幕滚动速度（0.5x-2x）
- **屏幕占比**：调整弹幕显示区域高度（20%-100%）
- **同屏数量**：设置同屏最大弹幕数量（10-100条）
- **弹幕颜色**：
  - 随机颜色：多彩弹幕
  - 白色：纯白色弹幕
  - 自定义：选择自己喜欢的颜色
- **清空弹幕**：一键清空屏幕上的所有弹幕

## 技术实现

### 前端库
- **vue3-danmaku**：弹幕渲染组件
- **Pinia**：状态管理，持久化配置到 localStorage

### 文件结构
```
src/
├── stores/
│   └── danmaku.ts          # 弹幕状态管理
├── components/
│   ├── DanmakuContainer.vue    # 弹幕容器
│   ├── DanmakuInput.vue        # 弹幕输入
│   └── DanmakuSettings.vue     # 弹幕设置面板
```

### 配置说明
弹幕配置会自动保存到浏览器的 localStorage 中，下次访问时会自动加载。

### 未来扩展
当前版本为本地弹幕功能，未来可扩展为：
- WebSocket 实时同步，实现跨用户实时弹幕
- 后端弹幕数据持久化
- 弹幕审核和过滤机制
- 用户身份认证

## 使用示例

1. 访问云上香网站
2. 在底部输入框输入祝福语，如"祈求家人平安"
3. 点击发送或按回车键
4. 弹幕会从右侧滑入屏幕
5. 点击设置按钮可调整弹幕效果

## 注意事项
- 弹幕功能默认启用，可在设置中关闭
- 弹幕内容遵循文明上网规范
- 请勿发送违规、攻击性内容
