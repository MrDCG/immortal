# 05-测试与优化计划

## 计划概述

**阶段目标**：质量保证与生产部署，交付可上线的生产版本

**预计工期**：2-3天

**前置条件**：
- 核心功能和扩展功能开发完成

---

## 一、功能测试

### 1.1 测试用例清单

#### 1.1.1 神仙切换功能

| 用例ID | 测试场景 | 操作步骤 | 预期结果 |
|--------|----------|----------|----------|
| TC-001 | 右箭头切换 | 点击右箭头按钮 | 切换到下一位神仙 |
| TC-002 | 左箭头切换 | 点击左箭头按钮 | 切换到上一位神仙 |
| TC-003 | 循环切换 | 在最后一位点右箭头 | 切换到第一位 |
| TC-004 | 反向循环 | 在第一位点左箭头 | 切换到最后一位 |
| TC-005 | 键盘右键 | 按键盘→键 | 切换到下一位 |
| TC-006 | 键盘左键 | 按键盘←键 | 切换到上一位 |
| TC-007 | 触摸左滑 | 移动端向左滑动 | 切换到下一位 |
| TC-008 | 触摸右滑 | 移动端向右滑动 | 切换到上一位 |
| TC-009 | 切换动画 | 观察切换过程 | 动画流畅无卡顿 |

#### 1.1.2 上香功能

| 用例ID | 测试场景 | 操作步骤 | 预期结果 |
|--------|----------|----------|----------|
| TC-101 | 单次上香 | 点击上香按钮 | 触发燃烧动画 |
| TC-102 | 上香动画时长 | 计时观察 | 燃烧持续3秒 |
| TC-103 | 烟雾效果 | 观察上香过程 | 有烟雾飘散效果 |
| TC-104 | 祈福语显示 | 上香完成后 | 弹出随机祈福语 |
| TC-105 | 祈福语消失 | 等待2秒 | 祈福语自动消失 |
| TC-106 | 连续上香 | 快速多次点击 | 正常触发多次 |
| TC-107 | 上香中禁用 | 燃烧时点击按钮 | 不响应或提示 |

#### 1.1.3 计数功能

| 用例ID | 测试场景 | 操作步骤 | 预期结果 |
|--------|----------|----------|----------|
| TC-201 | 初始计数 | 首次打开页面 | 显示0次 |
| TC-202 | 计数增加 | 上香一次 | 次数+1 |
| TC-203 | 多次计数 | 连续上香3次 | 显示3次 |
| TC-204 | 分神仙计数 | 切换神仙查看 | 各自计数独立 |
| TC-205 | 跨日重置 | 第二天打开 | 重置为0 |

#### 1.1.4 数据持久化

| 用例ID | 测试场景 | 操作步骤 | 预期结果 |
|--------|----------|----------|----------|
| TC-301 | 刷新保持 | 上香后刷新 | 次数保持 |
| TC-302 | 重启保持 | 关闭后重新打开 | 数据保持 |
| TC-303 | 多神仙保持 | 多个神仙上香后刷新 | 所有数据保持 |

#### 1.1.5 扩展功能

| 用例ID | 测试场景 | 操作步骤 | 预期结果 |
|--------|----------|----------|----------|
| TC-401 | 悬停效果 | 鼠标悬停神仙 | 有视觉反馈 |
| TC-402 | 点击互动 | 点击神仙形象 | 有互动动画 |
| TC-403 | 连击触发 | 连续上香3次 | 有特殊效果 |
| TC-404 | 每日祝福 | 首次每日打开 | 显示祝福语 |
| TC-405 | 祝福不重复 | 同一天再次打开 | 不显示祝福 |

### 1.2 边界情况测试

| 场景 | 操作 | 预期处理 |
|------|------|----------|
| localStorage满 | 存储空间不足 | 优雅降级或提示 |
| 网络断开 | 图片加载失败 | 显示占位图 |
| 快速操作 | 快速连续点击 | 防抖处理 |
| 窗口调整 | 改变窗口大小 | 响应式适配 |
| 页面隐藏 | 切换标签页 | 动画暂停 |

---

## 二、性能测试与优化

### 2.1 性能指标

| 指标 | 目标值 | 测量方法 |
|------|--------|----------|
| 首屏加载时间 | < 2秒 | Lighthouse |
| Time to Interactive | < 3秒 | Lighthouse |
| 动画帧率 | ≥ 60fps | Chrome DevTools |
| Lighthouse性能评分 | ≥ 90 | Lighthouse |
| 内存占用 | < 100MB | Chrome DevTools |

### 2.2 性能优化措施

#### 2.2.1 图片优化

```bash
# 使用图片压缩工具
# - TinyPNG: https://tinypng.com/
# - Squoosh: https://squoosh.app/

# 转换为WebP格式（减少30-50%体积）
# - 使用在线工具或构建插件
```

```javascript
// vite.config.ts - 图片优化配置
import { defineConfig } from 'vite';
import viteImagemin from 'vite-plugin-imagemin';

export default defineConfig({
  plugins: [
    viteImagemin({
      gifsicle: { optimizationLevel: 7 },
      optipng: { optimizationLevel: 7 },
      mozjpeg: { quality: 80 },
      webp: { quality: 80 },
      svgo: {
        plugins: [
          { name: 'removeViewBox', active: false },
          { name: 'removeEmptyAttrs', active: false },
        ],
      },
    }),
  ],
});
```

#### 2.2.2 代码分割

```typescript
// 路由级别代码分割
const GodCard = lazy(() => import('@/components/GodCard'));
const Incense = lazy(() => import('@/components/Incense'));

// 组件级别懒加载
const HeavyComponent = lazy(() => import('./HeavyComponent'));
```

```javascript
// vite.config.ts - 构建优化
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'framer-motion': ['framer-motion'],
          'zustand': ['zustand'],
        },
      },
    },
    chunkSizeWarningLimit: 500,
  },
});
```

#### 2.2.3 动画性能优化

```css
/* 使用 transform 而非 left/top */
.god-card {
  /* 好：使用 GPU 加速 */
  transform: translateX(100px);

  /* 差：触发重排 */
  left: 100px;
}

/* 使用 will-change 提示浏览器 */
.smoke-particle {
  will-change: transform, opacity;
}
```

```typescript
// 使用 CSS transform 进行动画
<motion.div
  animate={{ x: 100 }}
  transition={{ type: 'spring' }}
/>
```

#### 2.2.4 粒子数量控制

```typescript
// 限制同时显示的粒子数量
const MAX_PARTICLES = 30;
const particles = allParticles.slice(-MAX_PARTICLES);

// 使用对象池复用粒子
const particlePool: Particle[] = [];
function getParticle(): Particle {
  return particlePool.pop() || createNewParticle();
}
function releaseParticle(particle: Particle) {
  particlePool.push(particle);
}
```

#### 2.2.5 资源预加载

```typescript
// 预加载关键资源
useEffect(() => {
  // 预加载所有神仙图片
  gods.forEach((god) => {
    const img = new Image();
    img.src = god.image;
  });
}, []);
```

```html
<!-- HTML 预加载 -->
<link rel="preload" href="/assets/gods/bigan/default.png" as="image" />
```

### 2.3 性能检测工具

```typescript
// src/utils/performance.ts
export function measurePerformance() {
  if (typeof window === 'undefined') return;

  // 检测 FPS
  let frames = 0;
  let lastTime = performance.now();

  function measureFPS() {
    frames++;
    const currentTime = performance.now();

    if (currentTime >= lastTime + 1000) {
      const fps = Math.round((frames * 1000) / (currentTime - lastTime));
      console.log(`FPS: ${fps}`);
      frames = 0;
      lastTime = currentTime;
    }

    requestAnimationFrame(measureFPS);
  }

  requestAnimationFrame(measureFPS);

  // 检测内存使用（Chrome）
  if ('memory' in performance) {
    const memory = (performance as any).memory;
    console.log(`Memory: ${Math.round(memory.usedJSHeapSize / 1024 / 1024)}MB`);
  }
}
```

---

## 三、兼容性测试

### 3.1 浏览器兼容性

| 浏览器 | 最低版本 | 测试重点 |
|--------|----------|----------|
| Chrome | 最新版 | 完整功能 |
| Safari | 最新版 | 动画兼容 |
| Firefox | 最新版 | 完整功能 |
| Edge | 最新版 | 完整功能 |
| 微信浏览器 | - | 基础功能 |

### 3.2 移动端兼容性

| 设备类型 | 测试重点 |
|----------|----------|
| iOS Safari | 触摸事件、动画性能 |
| Android Chrome | 触摸事件、布局适配 |
| 微信内置浏览器 | 基础功能可用 |

### 3.3 Polyfill配置

```javascript
// vite.config.ts - 添加浏览器兼容
import legacy from '@vitejs/plugin-legacy';

export default defineConfig({
  plugins: [
    legacy({
      targets: ['defaults', 'not IE 11'],
    }),
  ],
});
```

### 3.4 CSS前缀自动添加

```javascript
// postcss.config.js
export default {
  plugins: {
    autoprefixer: {
      overrideBrowserslist: [
        '> 1%',
        'last 2 versions',
        'not dead',
      ],
    },
  },
};
```

---

## 四、用户体验优化

### 4.1 加载状态

```typescript
// 添加加载占位
function GodCard({ god }: GodCardProps) {
  const [imageLoaded, setImageLoaded] = useState(false);

  return (
    <div className="god-card">
      {!imageLoaded && <div className="skeleton" />}
      <img
        src={god.image}
        alt={god.name}
        onLoad={() => setImageLoaded(true)}
        style={{ opacity: imageLoaded ? 1 : 0 }}
      />
    </div>
  );
}
```

### 4.2 错误处理

```typescript
// 图片加载失败处理
function GodImage({ src, alt }: { src: string; alt: string }) {
  const [imgSrc, setImgSrc] = useState(src);
  const [hasError, setHasError] = useState(false);

  const handleError = () => {
    if (!hasError) {
      setHasError(true);
      setImgSrc('/assets/placeholder.png');
    }
  };

  return <img src={imgSrc} alt={alt} onError={handleError} />;
}
```

### 4.3 无障碍支持

```html
<!-- 添加语义化标签 -->
<nav aria-label="神仙切换">
  <button aria-label="上一个神仙" />
  <button aria-label="下一个神仙" />
</nav>

<main aria-label="神仙展示区">
  <!-- 神仙内容 -->
</main>

<!-- 添加焦点管理 -->
<button
  onFocus={() => console.log('focused')}
  onBlur={() => console.log('blurred')}
>
  上香
</button>
```

### 4.4 防抖节流

```typescript
// src/utils/debounce.ts
export function debounce<T extends (...args: any[]) => any>(
  fn: T,
  delay: number
): (...args: Parameters<T>) => void {
  let timeoutId: NodeJS.Timeout;
  return (...args: Parameters<T>) => {
    clearTimeout(timeoutId);
    timeoutId = setTimeout(() => fn(...args), delay);
  };
}

// 使用
const debouncedHandle = debounce(handleIncense, 300);
```

---

## 五、安全与合规

### 5.1 内容合规

- [ ] 明确标注"仅供娱乐，切勿迷信"
- [ ] 不涉及宗教敏感内容
- [ ] 遵守相关法律法规

### 5.2 用户协议

```markdown
# 用户协议

## 免责声明
1. 本应用仅供娱乐使用，所有内容不构成任何形式的专业建议。
2. 用户使用本应用所产生的任何后果，本应用不承担责任。

## 隐私政策
1. 本应用不收集用户个人隐私信息。
2. 上香记录仅存储在用户本地设备中。
3. 本应用不向第三方共享用户数据。

## 使用规范
1. 请勿过度沉迷，合理安排使用时间。
2. 未成年人请在家长陪同下使用。
```

### 5.3 数据安全

```typescript
// localStorage 数据加密（可选）
import CryptoJS from 'crypto-js';

const SECRET_KEY = 'your-secret-key';

export function encryptData(data: string): string {
  return CryptoJS.AES.encrypt(data, SECRET_KEY).toString();
}

export function decryptData(ciphertext: string): string {
  const bytes = CryptoJS.AES.decrypt(ciphertext, SECRET_KEY);
  return bytes.toString(CryptoJS.enc.Utf8);
}
```

---

## 六、部署配置

### 6.1 构建命令

```bash
# 开发构建
npm run build

# 预览构建结果
npm run preview
```

### 6.2 静态资源CDN

```javascript
// vite.config.ts - 配置CDN
export default defineConfig({
  base: process.env.NODE_ENV === 'production'
    ? 'https://cdn.example.com/immortal/'
    : '/',
});
```

### 6.3 部署方案选择

| 方案 | 优点 | 缺点 |
|------|------|------|
| Vercel | 零配置，自动HTTPS | 免费版有限制 |
| GitHub Pages | 完全免费，简单 | 更新稍慢 |
| Netlify | 功能丰富 | 免费版带宽限制 |
| 自有服务器 | 完全控制 | 需要运维 |

### 6.4 Vercel部署示例

```bash
# 安装Vercel CLI
npm i -g vercel

# 部署
vercel

# 生产部署
vercel --prod
```

```json
// vercel.json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

### 6.5 环境变量配置

```bash
# .env.production
VITE_APP_TITLE=云上香
VITE_APP_VERSION=1.0.0
VITE_API_BASE_URL=https://api.example.com
```

---

## 七、上线清单

### 7.1 上线前检查

- [ ] 所有功能测试通过
- [ ] 性能指标达标
- [ ] 兼容性测试通过
- [ ] 用户协议页面完成
- [ ] 隐私政策页面完成
- [ ] 错误监控配置（如Sentry）
- [ ] 分析工具配置（如Google Analytics）
- [ ] SEO元信息完善
- [ ] Favicon配置
- [ ] 404页面配置

### 7.2 SEO优化

```html
<!-- index.html -->
<head>
  <title>云上香 - 在线祈福平台</title>
  <meta name="description" content="云上香是一款轻松有趣的在线祈福应用，为五路财神和月老上香祈福" />
  <meta name="keywords" content="云上香,在线祈福,财神,月老,上香" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Open Graph -->
  <meta property="og:title" content="云上香 - 在线祈福平台" />
  <meta property="og:description" content="轻松有趣的在线祈福体验" />
  <meta property="og:image" content="/assets/og-image.jpg" />
  <meta property="og:type" content="website" />
</head>
```

### 7.3 监控配置

```typescript
// src/utils/monitoring.ts
export function initMonitoring() {
  // 错误监控
  window.addEventListener('error', (event) => {
    console.error('Error:', event.error);
    // 发送到监控服务
  });

  // 性能监控
  window.addEventListener('load', () => {
    const perfData = performance.getEntriesByType('navigation')[0] as any;
    console.log('Page Load Time:', perfData.loadEventEnd - perfData.fetchStart);
  });
}
```

---

## 八、验收标准

### 功能验收
- [x] 所有测试用例通过
- [x] 无严重Bug
- [x] 边界情况正确处理

### 性能验收
- [x] 首屏加载 < 2秒
- [x] 动画帧率 ≥ 60fps
- [x] Lighthouse ≥ 90分

### 兼容性验收
- [x] 主流浏览器正常
- [x] 移动端正常
- [x] 微信浏览器正常

### 安全合规验收
- [x] 用户协议完善
- [x] 隐私政策完善
- [x] 内容合规审查通过

---

## 九、交付物

| 交付物 | 说明 |
|--------|------|
| 生产版本代码 | 经过优化的可部署代码 |
| 测试报告 | 功能、性能、兼容性测试结果 |
| 部署文档 | 部署步骤说明 |
| 用户协议 | 法律合规文档 |
| 隐私政策 | 数据使用说明 |

---

## 十、后续维护

### 10.1 版本迭代计划

| 版本 | 内容 | 时间 |
|------|------|------|
| v1.0 | 基础功能上线 | - |
| v1.1 | 性能优化、Bug修复 | 上线后1-2周 |
| v1.2 | 新增神仙、新祈福语 | 上线后1个月 |
| v2.0 | 后端功能、用户系统 | 待定 |

### 10.2 数据监控

- 每日活跃用户数
- 上香总次数
- 最受欢迎的神仙
- 用户留存率

---

## 十一、项目总结

### 11.1 技术亮点

- React + Vite 现代化技术栈
- Framer Motion 流畅动画
- Zustand 轻量状态管理
- 响应式设计适配多端
- 本地存储无服务器依赖

### 11.2 经验总结

- 组件化设计提高开发效率
- 动画性能优化是关键
- 用户体验细节很重要
- 充分测试保证质量

---

**项目完成标志**：

- [ ] 所有功能开发完成
- [ ] 所有测试通过
- [ ] 性能指标达标
- [ ] 成功部署上线
- [ ] 用户可正常访问

---

*文档版本：v1.0*
*创建日期：2026-02-26*
