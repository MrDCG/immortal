# 01-项目初始化计划

## 计划概述

**阶段目标**：搭建可运行的项目框架，完成开发环境配置

**预计工期**：1-2天

**前置条件**：
- Node.js >= 18 已安装
- 包管理器（npm/pnpm/yarn）可用
- 代码编辑器（VS Code推荐）

---

## 一、项目脚手架搭建

### 1.1 创建Vite + Vue 3 + TypeScript项目

```bash
# 使用npm
npm create vite@latest immortal -- --template vue-ts

# 或使用pnpm
pnpm create vite immortal --template vue-ts

# 或使用yarn
yarn create vite immortal --template vue-ts
```

### 1.2 进入项目目录并安装依赖

```bash
cd immortal
npm install
```

---

## 二、依赖包安装

### 2.1 核心依赖

```bash
# 动画库
npm install @vueuse/core

# 状态管理
npm install pinia

# 样式方案
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

### 2.2 开发工具依赖

```bash
# ESLint配置
npm install -D eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin

# 代码格式化
npm install -D prettier eslint-config-prettier eslint-plugin-prettier

# 类型检查
npm install -D @types/node

# Git钩子（可选）
npm install -D husky lint-staged
```

### 2.3 其他工具库

```bash
# 日期处理（可选）
npm install date-fns

# VueUse 已包含触摸手势功能，无需额外安装
```

---

## 三、项目目录结构创建

### 3.1 完整目录结构

```bash
# 创建src子目录
mkdir -p src/components/GodCard
mkdir -p src/components/Incense
mkdir -p src/components/BlessingPopup
mkdir -p src/components/Navigation
mkdir -p src/components/Counter
mkdir -p src/components/DailyGreeting
mkdir -p src/hooks
mkdir -p src/store
mkdir -p src/data
mkdir -p src/types
mkdir -p src/styles
mkdir -p src/utils

# 创建public资源目录
mkdir -p public/assets/gods/bigan
mkdir -p public/assets/gods/guanyu
mkdir -p public/assets/gods/chai
mkdir -p public/assets/gods/zha
mkdir -p public/assets/gods/wanghai
mkdir -p public/assets/gods/yuelao
mkdir -p public/assets/sounds
mkdir -p public/assets/effects
```

### 3.2 目录结构说明

| 目录 | 说明 |
|------|------|
| `src/components/` | Vue组件目录 |
| `src/composables/` | Vue组合式函数 |
| `src/stores/` | Pinia状态管理 |
| `src/data/` | 静态数据（神仙数据、祈福语） |
| `src/types/` | TypeScript类型定义 |
| `src/assets/styles/` | 全局样式文件 |
| `src/utils/` | 工具函数 |
| `public/assets/` | 静态资源文件 |

---

## 四、配置文件设置

### 4.1 Tailwind CSS配置

**文件**: `tailwind.config.js`

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{vue,js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // 神仙主题色
        'wealth-gold': '#D4AF37',
        'love-pink': '#FF69B4',
      },
      animation: {
        // 自定义动画
        'float': 'float 3s ease-in-out infinite',
        'smoke': 'smoke 3s ease-out forwards',
      },
      keyframes: {
        float: {
          '0%, 100%': { transform: 'translateY(0)' },
          '50%': { transform: 'translateY(-10px)' },
        },
        smoke: {
          '0%': { opacity: '0.8', transform: 'translateY(0) scale(1)' },
          '100%': { opacity: '0', transform: 'translateY(-100px) scale(2)' },
        },
      },
    },
  },
  plugins: [],
}
```

**文件**: `src/assets/styles/main.css`

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* 全局样式 */
:root {
  --color-wealth: #D4AF37;
  --color-love: #FF69B4;
  --color-bg-light: #FFF8E7;
  --color-bg-dark: #1a1a2e;
}

body {
  margin: 0;
  padding: 0;
  font-family: 'PingFang SC', 'Microsoft YaHei', sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

#app {
  min-height: 100vh;
}

/* 禁用移动端双击缩放 */
* {
  touch-action: manipulation;
}
```

### 4.2 TypeScript配置

**文件**: `tsconfig.json`

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"],
      "@components/*": ["./src/components/*"],
      "@composables/*": ["./src/composables/*"],
      "@stores/*": ["./src/stores/*"],
      "@data/*": ["./src/data/*"],
      "@types/*": ["./src/types/*"],
      "@assets/*": ["./src/assets/*"],
      "@utils/*": ["./src/utils/*"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.tsx", "src/**/*.vue"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

### 4.3 Vite配置

**文件**: `vite.config.ts`

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import path from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
      '@components': path.resolve(__dirname, './src/components'),
      '@composables': path.resolve(__dirname, './src/composables'),
      '@stores': path.resolve(__dirname, './src/stores'),
      '@data': path.resolve(__dirname, './src/data'),
      '@types': path.resolve(__dirname, './src/types'),
      '@assets': path.resolve(__dirname, './src/assets'),
      '@utils': path.resolve(__dirname, './src/utils'),
    },
  },
  build: {
    // 资源内联阈值
    assetsInlineLimit: 4096,
    // 代码分割
    rollupOptions: {
      output: {
        manualChunks: {
          'vue-vendor': ['vue', 'vue-router'],
          'pinia': ['pinia'],
          'vueuse': ['@vueuse/core'],
        },
      },
    },
  },
  server: {
    port: 3000,
    open: true,
  },
})
```

### 4.4 ESLint配置

**文件**: `.eslintrc.cjs`

```javascript
module.exports = {
  root: true,
  env: {
    browser: true,
    es2020: true,
  },
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:vue/vue3-recommended',
  ],
  ignorePatterns: ['dist', '.eslintrc.cjs'],
  parser: 'vue-eslint-parser',
  parserOptions: {
    ecmaVersion: 'latest',
    parser: '@typescript-eslint/parser',
    sourceType: 'module',
  },
  plugins: ['vue', '@typescript-eslint'],
  rules: {
    'vue/multi-word-component-names': 'off',
    '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
  },
}
```

---

## 五、基础代码框架

### 5.1 类型定义文件

**文件**: `src/types/index.ts`

```typescript
// 神仙类型
export interface God {
  id: string;
  name: string;
  title: string;
  direction: string;
  type: 'wealth' | 'love';
  image: string;
  bgImage?: string;
  color: string;
  blessings: string[];
}

// 上香记录类型
export interface IncenseRecord {
  godId: string;
  date: string;
  count: number;
}

// 应用状态类型
export interface AppState {
  currentGodIndex: number;
  gods: God[];
  incenseRecords: Record<string, IncenseRecord>;
}
```

### 5.2 神仙数据文件

**文件**: `src/data/gods.ts`

```typescript
import { God } from '@/types';

export const gods: God[] = [
  {
    id: 'bigan',
    name: '比干',
    title: '东路财神',
    direction: '东',
    type: 'wealth',
    image: '/assets/gods/bigan/default.png',
    color: '#D4AF37',
    blessings: ['暴富', '财源滚滚', '金来'],
  },
  {
    id: 'guanyu',
    name: '关羽',
    title: '西路财神',
    direction: '西',
    type: 'wealth',
    image: '/assets/gods/guanyu/default.png',
    color: '#D4AF37',
    blessings: ['生意兴隆', '财源广进', '招财进宝'],
  },
  {
    id: 'chai',
    name: '柴王爷',
    title: '南路财神',
    direction: '南',
    type: 'wealth',
    image: '/assets/gods/chai/default.png',
    color: '#D4AF37',
    blessings: ['钱从四面八方来', '日进斗金', '财气冲天'],
  },
  {
    id: 'zha',
    name: '赵公明',
    title: '北路财神',
    direction: '北',
    type: 'wealth',
    image: '/assets/gods/zha/default.png',
    color: '#D4AF37',
    blessings: ['财运亨通', '黄金万两', '富贵逼人'],
  },
  {
    id: 'wanghai',
    name: '王亥',
    title: '中路财神',
    direction: '中',
    type: 'wealth',
    image: '/assets/gods/wanghai/default.png',
    color: '#D4AF37',
    blessings: ['五路来财', '八方聚财', '大吉大利'],
  },
  {
    id: 'yuelao',
    name: '月老',
    title: '月下老人',
    direction: '',
    type: 'love',
    image: '/assets/gods/yuelao/default.png',
    color: '#FF69B4',
    blessings: ['爱情速来', '良缘已到', '桃花朵朵', '红线牵缘'],
  },
];

export const defaultGodIndex = 0;
```

### 5.3 状态管理Store

**文件**: `src/stores/incense.ts`

```typescript
import { defineStore } from 'pinia';
import { ref, computed } from 'vue';
import type { God, IncenseRecord } from '@/types';

export const useIncenseStore = defineStore('incense', () => {
  // 状态
  const currentGodIndex = ref(0);
  const incenseRecords = ref<Record<string, IncenseRecord>>({});

  // 计算属性
  const currentGod = computed(() => (gods: God[]) => {
    return gods[currentGodIndex.value] || null;
  });

  // 方法
  const setCurrentGodIndex = (index: number) => {
    currentGodIndex.value = index;
  };

  const nextGod = () => {
    currentGodIndex.value = (currentGodIndex.value + 1) % 6;
  };

  const prevGod = () => {
    currentGodIndex.value = (currentGodIndex.value - 1 + 6) % 6;
  };

  const addIncense = (godId: string) => {
    const today = new Date().toISOString().split('T')[0];
    const key = `${godId}_${today}`;
    incenseRecords.value = {
      ...incenseRecords.value,
      [key]: {
        godId,
        date: today,
        count: (incenseRecords.value[key]?.count || 0) + 1,
      },
    };
  };

  const getTodayIncenseCount = (godId: string): number => {
    const today = new Date().toISOString().split('T')[0];
    const key = `${godId}_${today}`;
    return incenseRecords.value[key]?.count || 0;
  };

  const getCurrentGod = (gods: God[]): God | null => {
    return gods[currentGodIndex.value] || null;
  };

  return {
    currentGodIndex,
    incenseRecords,
    setCurrentGodIndex,
    nextGod,
    prevGod,
    addIncense,
    getTodayIncenseCount,
    getCurrentGod,
  };
}, {
  persist: {
    key: 'incense-storage',
  },
});
```

### 5.4 主入口文件

**文件**: `src/main.ts`

```typescript
import { createApp } from 'vue';
import { createPinia } from 'pinia';
import App from './App.vue';
import './assets/styles/main.css';

const app = createApp(App);
const pinia = createPinia();

app.use(pinia);
app.mount('#app');
```

**文件**: `src/App.vue`

```vue
<template>
  <div class="min-h-screen bg-gradient-to-br from-amber-50 to-orange-100">
    <DailyGreeting />
    <Counter />
    <Transition name="fade" mode="out-in">
      <GodCard :key="currentGodIndex" />
    </Transition>
    <Incense />
    <Navigation />
    <BlessingPopup />
  </div>
</template>

<script setup lang="ts">
import { storeToRefs } from 'pinia';
import { useIncenseStore } from '@/stores/incense';
import GodCard from '@/components/GodCard.vue';
import Navigation from '@/components/Navigation.vue';
import Counter from '@/components/Counter.vue';
import Incense from '@/components/Incense.vue';
import BlessingPopup from '@/components/BlessingPopup.vue';
import DailyGreeting from '@/components/DailyGreeting.vue';

const incenseStore = useIncenseStore();
const { currentGodIndex } = storeToRefs(incenseStore);
</script>

<style>
.fade-enter-active,
.fade-leave-active {
  transition: opacity 0.3s ease, transform 0.3s ease;
}

.fade-enter-from {
  opacity: 0;
  transform: translateX(30px);
}

.fade-leave-to {
  opacity: 0;
  transform: translateX(-30px);
}
</style>
```

---

## 六、组件骨架创建

### 6.1 创建组件骨架文件

**GodCard组件** - `src/components/GodCard.vue`
```vue
<template>
  <div>神仙卡片组件</div>
</template>

<script setup lang="ts">
</script>
```

**Navigation组件** - `src/components/Navigation.vue`
```vue
<template>
  <div>切换导航组件</div>
</template>

<script setup lang="ts">
</script>
```

**Incense组件** - `src/components/Incense.vue`
```vue
<template>
  <div>上香组件</div>
</template>

<script setup lang="ts">
</script>
```

**BlessingPopup组件** - `src/components/BlessingPopup.vue`
```vue
<template>
  <div>祈福语弹窗组件</div>
</template>

<script setup lang="ts">
</script>
```

**Counter组件** - `src/components/Counter.vue`
```vue
<template>
  <div>计数器组件</div>
</template>

<script setup lang="ts">
</script>
```

**DailyGreeting组件** - `src/components/DailyGreeting.vue`
```vue
<template>
  <div>每日祝福组件</div>
</template>

<script setup lang="ts">
</script>
```

---

## 七、验证测试

### 7.1 启动开发服务器

```bash
npm run dev
```

### 7.2 验证清单

- [ ] 项目成功启动，访问 http://localhost:3000
- [ ] 页面正常显示，无控制台错误
- [ ] TypeScript类型检查通过
- [ ] Tailwind CSS样式生效
- [ ] 热更新（HMR）正常工作
- [ ] ESLint配置生效

### 7.3 构建测试

```bash
npm run build
```

- [ ] 生产构建成功
- [ ] 预览构建结果正常 `npm run preview`

---

## 八、交付物

| 交付物 | 说明 |
|--------|------|
| 可运行的项目框架 | npm install后可直接运行 |
| 完整的目录结构 | 按规划的目录创建完成 |
| 配置文件 | 所有配置文件完成设置 |
| 组件骨架 | 基础组件文件创建 |
| 类型定义 | TypeScript类型定义完成 |

---

## 九、下一步

完成项目初始化后，进入**阶段二：UI设计与资源准备**

详见：[02-UI设计与资源准备计划.md](./02-UI设计与资源准备计划.md)

---

*文档版本：v1.0*
*创建日期：2026-02-26*
